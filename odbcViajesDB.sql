-- MySQL Script generated by MySQL Workbench
-- Wed Aug 28 10:35:05 2019
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='TRADITIONAL,ALLOW_INVALID_DATES';

-- -----------------------------------------------------
-- Schema odbcViajes
-- -----------------------------------------------------

-- -----------------------------------------------------
-- Schema odbcViajes
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `odbcViajes` DEFAULT CHARACTER SET utf8 ;
USE `odbcViajes` ;

-- -----------------------------------------------------
-- Table `odbcViajes`.`Pasajero`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `odbcViajes`.`Pasajero` (
  `DNI` INT NOT NULL,
  `nombre` VARCHAR(45) NOT NULL,
  `apellido` VARCHAR(45) NOT NULL,
  `telefono` VARCHAR(45) NOT NULL,
  `email` VARCHAR(45) NOT NULL,
  `millas` FLOAT NULL,
  `clave` VARCHAR(45) NULL,
  `direccion` VARCHAR(45) NOT NULL,
  `nacionalidad` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`DNI`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `odbcViajes`.`Ciudad`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `odbcViajes`.`Ciudad` (
  `idCiudad` INT NOT NULL AUTO_INCREMENT,
  `nombre` VARCHAR(45) NOT NULL,
  `coordenada` VARCHAR(45) NOT NULL,
  `baja` TINYINT NOT NULL DEFAULT 0,
  PRIMARY KEY (`idCiudad`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `odbcViajes`.`Cancelacion`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `odbcViajes`.`Cancelacion` (
  `idCancelacion` INT NOT NULL AUTO_INCREMENT,
  `fecha` DATE NULL,
  `montoReintegro` FLOAT NOT NULL,
  PRIMARY KEY (`idCancelacion`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `odbcViajes`.`Pasaje`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `odbcViajes`.`Pasaje` (
  `codigo` VARCHAR(10) NOT NULL,
  `fecha` DATE NULL,
  `valor` FLOAT NOT NULL,
  `pasajero` INT NOT NULL,
  `origen` INT NOT NULL,
  `destino` INT NOT NULL,
  `formaPago` ENUM('dinero', 'millas') NULL,
  `cancelacion` INT NULL,
  PRIMARY KEY (`codigo`),
  INDEX `fk_Pasaje_Pasajero_idx` (`pasajero` ASC),
  INDEX `fk_Pasaje_Destinos1_idx` (`origen` ASC),
  INDEX `fk_Pasaje_Destinos2_idx` (`destino` ASC),
  INDEX `fk_Pasaje_Cancelacion1_idx` (`cancelacion` ASC),
  CONSTRAINT `fk_Pasaje_Pasajero`
    FOREIGN KEY (`pasajero`)
    REFERENCES `odbcViajes`.`Pasajero` (`DNI`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_Pasaje_Destinos1`
    FOREIGN KEY (`origen`)
    REFERENCES `odbcViajes`.`Ciudad` (`idCiudad`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_Pasaje_Destinos2`
    FOREIGN KEY (`destino`)
    REFERENCES `odbcViajes`.`Ciudad` (`idCiudad`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_Pasaje_Cancelacion1`
    FOREIGN KEY (`cancelacion`)
    REFERENCES `odbcViajes`.`Cancelacion` (`idCancelacion`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
